/**
 * Re-export shim — implementation moved to @mortly/core (shared with the
 * mobile app). Keeps the web's existing @/lib/brokerEntitlement imports working.
 */
export * from "@mortly/core/brokerEntitlement";
