# Mortly Launch-Readiness Fix Plan

**Created:** 2026-06-13 · **Source:** `MORTLY_FEATURE_REVIEW.md` (163 issues, 14 launch blockers, NO-GO verdict)
**How to resume in a future session:** read this file top-to-bottom, check the checkboxes against `git log --oneline` since 2026-06-13, run `npx tsc --noEmit && npm test` to confirm green, then continue at the first unchecked item. Issue numbers (C-1, H-1…H-21) refer to `MORTLY_FEATURE_REVIEW.md` §4–5.

**Working rules:** fix in small commits per item; every code fix gets/updates a test where a test suite exists for that layer; never edit `public/locales` for one locale without the other; run `npx tsc --noEmit` + targeted vitest before checking an item off.

---

## Stage 0 — Infrastructure & config (do first; blocks everything)

- [ ] **0.1 [USER ACTION — cannot be done from code] Verify Supabase Realtime hardening (C-1, Critical).** In Supabase SQL editor run:
  ```sql
  select relname, relrowsecurity from pg_class where relname in ('messages','conversations');
  select * from pg_policies where tablename in ('messages','conversations');
  select * from pg_publication_tables where tablename in ('messages','conversations');
  ```
  Required: `relrowsecurity = true` for both; NO anon policies; NO rows in the publication. If publication rows exist: `alter publication supabase_realtime drop table public.messages, public.conversations;`
- [ ] **0.2 [USER ACTION] Verify Vercel production env vars:** `KV_REST_API_URL` (+token), `STRIPE_PRICE_BASIC/PRO/PREMIUM`, `STRIPE_WEBHOOK_SECRET`, `CRON_SECRET`, `NEXTAUTH_URL`, `NEXTAUTH_SECRET`, `RESEND_API_KEY`, `RESEND_FROM`, `NEXT_PUBLIC_SUPABASE_URL/ANON_KEY`. (H-class: KV blocker #12, silent Stripe disable, broken reset links.)
- [x] **0.x Auto-apply migrations on prod deploy.** `build` script now runs `prisma migrate deploy` but ONLY when `VERCEL_ENV=production` (preview/local/CI skip it, so they never migrate a DB they shouldn't). This means the appleId migration + all future migrations apply automatically on the next production deploy — no manual step. PREREQ: `DATABASE_URL`/`DIRECT_URL` set in Vercel prod env. Standard Prisma deploy pattern. Note: it applies whatever SQL is in the migration files; destructive migrations still need review + a backup first.
- [x] **0.3 Create the missing `appleId` migration (H-1, blocker).** `prisma/migrations/20260613000000_add_apple_id/migration.sql` with `ALTER TABLE "users" ADD COLUMN "appleId" TEXT; CREATE UNIQUE INDEX "users_appleId_key" ON "users"("appleId");` — **[USER ACTION after deploy]** run `npx prisma migrate resolve --applied 20260613000000_add_apple_id` against prod if the column already exists there via `db push` (check first with `npx prisma migrate status`).
- [x] **0.4 First-ADMIN bootstrap script.** `scripts/create-admin.ts` — `ADMIN_EMAIL=... ADMIN_PASSWORD=... npx tsx scripts/create-admin.ts`. Idempotent (promotes an existing user instead of duplicating; bumps tokenVersion on promotion).

## Stage 1 — Funnel & request lifecycle (blockers 3,4,5,7 + High filter/cap)

- [x] **1.1 Fix blank `/borrower/request/new` for logged-out visitors (H-2, blocker).** Render `BorrowerShell` (which owns the redirect) instead of `return null`; add `callbackUrl` to BorrowerShell + BrokerShell login redirects so visitors come back to the form after auth.
- [x] **1.2 Add Close Request UI (H-4, blocker).** Button + confirm on `pages/borrower/request/[id].tsx` for OPEN/IN_PROGRESS calling existing `PUT {status:'CLOSED'}`; localized strings in both catalogs. Also add the missing current-status guard server-side (Low: close from terminal states).
- [x] **1.3 Fix edit-409 dead end (H-3, blocker).** Diff the form payload against the loaded request in `handleEdit` (send only changed keys); when `_count.conversations > 0` show a localized notice that only notes/timeline are editable; map the 409 to a translated message.
- [x] **1.4 Gate conversation creation on request status (H-10, blocker).** In `POST /api/conversations` (both broker & borrower branches): allow only `OPEN`/`IN_PROGRESS`, return 409 with error code; no credit spend on refusal.
- [x] **1.5 (partial) Expiry no longer kills negotiated requests (H-11).** `expire-requests` now skips requests with ACTIVE conversations. **DEFERRED [PRODUCT DECISION]:** auto-flipping `OPEN → IN_PROGRESS` on first conversation was NOT implemented — the broker marketplace browse and request-detail access gates only treat `OPEN` as visible/contactable, so the flip would hide a request from all other brokers after the first response and kill the multi-broker comparison model. Decide: (a) keep requests OPEN and drop IN_PROGRESS from borrower-facing copy, or (b) make browse/detail/conversation gates treat IN_PROGRESS like OPEN, then add the flip.
- [x] **1.6 Fix broker browse (H-5, H-6).** Filter on `hasMyConversation` (and add it to the frontend type); update `tests/broker/BrokerRequests.test.tsx` fixture to the real API shape; consume the `pagination` envelope with a Load-more button.

## Stage 2 — Money correctness (blockers 8,9,10,13,14)

- [x] **2.1 Scheduled downgrade billing (H-8, blocker).** Downgrades now create/update a Stripe Subscription Schedule (current price until period end, new price after, `proration_behavior:'none'`, `end_behavior:'release'`) so the renewal invoice bills the NEW price; `pendingTier` kept for the entitlement swap on `invoice.paid` (price swap there is now a legacy fallback, conditional on price mismatch). Upgrades release any schedule first (Stripe blocks direct updates on scheduled subs). Tests updated + new upgrade-after-scheduled-downgrade test.
- [x] **2.2 Cut PAST_DUE entitlements (H-9).** In `POST /api/conversations`, block when subscription status is `PAST_DUE`/`EXPIRED` (all tiers, incl. PREMIUM bypass).
- [x] **2.3 Clear stale `pendingTier`.** Cleared on `customer.subscription.deleted`, on new `checkout.session.completed` (update branch), and on upgrade (with schedule release). Server now also supports cancel-a-scheduled-downgrade by re-selecting the current tier (`{updated:true, cancelled:true}`) — **UI affordance for this still pending** (billing page blocks same-tier clicks client-side; add a "keep my plan" button to the pendingDowngrade banner — folded into 5.6).
- [x] **2.4 Billing UX (blockers 13,14).** Error banner for checkout/portal failures in `pages/broker/billing.tsx` (incl. the no-url silent path); added `broker.planUpdating` / `planUpdatePending` / `planUpdateFailed` / `portalFailed` to BOTH locale files. Bonus: fixed billing.tsx's own blank-page `return null` auth-gate bug (same class as H-2). Invoice-fetch error surfacing still pending (folded into 5.2).
- [x] **2.5 Honest pricing page (H-17, blocker) — pricing.tsx part.** Removed unimplemented `notifications`/`realtimeAlerts` rows (cards + comparison table) and the fabricated "was $49/$99/$199 + N% OFF" anchors. **REMAINING [USER/MARKETING DECISION]:** homepage "live" stats (500+/50+/95% in the LIVE-labeled card on index.tsx) — make real, relabel as goals, or remove.
- [x] **2.6 Webhook idempotency ordering (Medium).** Restructured to check-first → process → record-on-success: a crash/timeout mid-handler now leaves NO ledger row, so Stripe's retry reprocesses instead of being swallowed as a duplicate. Safe because all five handlers are internally idempotent (in-transaction period/status re-checks), which also covers the check→record race on concurrent deliveries. Removed the fragile rollback-delete + CRITICAL-alert path.

## Stage 3 — Bilingual integrity (blocker 6 + High i18n)

- [x] **3.1 Backfill missing i18n keys (H-7, blocker) — user-facing part.** `scripts/scan-i18n-keys.mjs` found 383 missing (97 user-facing + 286 admin.*). All 97 user-facing keys backfilled with proper ko+en values; the 3 Korean values in the EN catalog (consultation.step1-3) translated. **DECISION (documented in test):** admin.* exempt — admin console is Korean-only for launch; remove the exemption in tests/i18n/key-coverage.test.ts when admin gets EN support.
- [x] **3.2 CI guard.** `tests/i18n/key-coverage.test.ts` (runs in `npm test`; fails on any user-facing key missing from either catalog; vitest.config include extended). CI workflow now gates on `lint` + `tsc --noEmit` + `next build` (build verified locally: 50/50 pages). Also fixed `npm run lint` — `next lint` was REMOVED in Next 16 and the script was broken; now calls eslint directly; cleared all 22 pre-existing eslint errors (+ `^_` ignore patterns in .eslintrc.json).
- [ ] **3.3 Localized API errors.** STARTED: error codes now exist on the new/changed endpoints (`EDIT_LOCKED_BY_CONVERSATIONS`, `CLOSE_INVALID_STATUS`, `REQUEST_NOT_OPEN`, `SUBSCRIPTION_PAST_DUE`) and the request-detail page translates the edit-409. REMAINING: systematic codes + client translation for the other pages that render server prose verbatim (login, signup, request form, profile, onboarding, billing, reset).
- [x] **3.4 Locale plumbing (H-15, H-20 + Mediums) — except system messages.** Done: `Btn` internal links render `next/link` (locale preserved; external hrefs still `<a>`); reset email + link use the requester's UI locale (page sends `i18n.language`; `/en`-prefixed reset URL); locale switcher sets a 1-year `NEXT_LOCALE` cookie **and `localeDetection` is now `true`** (required for the cookie to work — side effect: English-browser first-timers land on /en; revert to `false` in next-i18next.config.js for strict Korean-first); all 8 live hardcoded `en-CA` date sites now thread the active locale (`lib/format.ts dateLocale()`). REMAINING (moved to 4.5): unify system-chat-message localization.

## Stage 4 — Notification layer (H-13, H-14, H-16, H-19 + Mediums)

- [x] **4.1 AdminNotice send path (H-14).** `POST /api/admin/notices` (withAdmin, validated, SEND_NOTICE audit row, accepts id or publicId) + send-notice button & modal on `/admin/users/[id]` — the command palette's 관리자 공지 발송 action no longer dead-ends.
- [x] **4.2 Request approve/reject notifications (H-13).** New `lib/notify.ts notifyUser()` (AdminNotice + Expo push + bilingual lifecycle email via new `sendLifecycleEmail`; fire-safe, never throws) wired into APPROVE_REQUEST / REJECT_REQUEST in `pages/api/admin/requests/[id].ts`, rejectionReason included in both languages.
- [x] **4.3 Broker verification decision notifications.** Same `notifyUser` pattern on VERIFIED / REJECTED transitions in `pages/api/admin/brokers/[id].ts` (only fires on actual status change).
- [x] **4.4 Payment-failed notification (H-16, blocker).** `sendPaymentFailedEmail` (bilingual, billing-portal CTA) + AdminNotice with `dedupeKey = payment-failed-<invoiceId>` (at-most-once across Stripe retries; attributed to the first ADMIN account) in `handleInvoicePaymentFailed`, fire-safe. This is also the FIRST AdminNotice writer — the Navbar bell now has real content. Push notification still pending (needs a billing push template). **[USER ACTION]** also enable Stripe dashboard dunning emails.
- [x] **4.5 Cron events notify (Medium).** Auto-close: every closed conversation now gets a bilingual system message + realtime nudge; the broken engagement filter replaced with the exact `borrowerMsgCount: 0` DB predicate (the old `senderId != "SYSTEM"` excluded nothing and the unordered `take:10` sample could misclassify engaged borrowers). Expire-requests: selects affected requests first, then notifies each borrower (notice + push, `dedupeKey = request-expired-<id>`), bounded at 1000/run. Cron tests updated.
- [x] **4.6 Mobile notifications (H-19) — Navbar part.** Notice list extracted into a shared `NoticeList`; mobile menu button now opens a real full-width sheet (backdrop, close button) instead of toggling state that rendered nothing. REMAINING (folded into 5.x polish): a notices affordance inside Borrower/Broker shells (authed pages don't mount the marketing Navbar).

## Stage 5 — Robustness & error states (top Mediums)

- [x] 5.1 Error-vs-empty state: `requestsError` exposed from both data contexts; borrower + broker dashboards now show errors instead of empty/all-caught-up on fetch failure; admin people list now distinguishes load-failure from "no matches" (reports/activity lists follow the same useSWR pattern if needed — REMAINING, low priority). Retry buttons still pending.
- [x] 5.2 Request-detail infinite skeleton → actionable error + back link. resend-code false-success FIXED (any non-429 failure now shows an error instead of "code sent"; 429 also labeled). REMAINING: billing/profile retry affordances.
- [x] 5.3 (done) Closed-conversation send guard (409 `CONVERSATION_CLOSED` in POST /api/messages) + `isSystem` now selected in conversation GET and rendered as centered system notices in both chat UIs (was misattributed as participant bubbles). +1 test.
- [ ] 5.3 Closed-conversation send guard (409) + poll also refreshes conversation status; return `isSystem` in conversation payload and render system bubbles.
- [ ] 5.4 Pagination: borrower context (limit + totals from envelope), chat history (load older — NOTE: GET /api/conversations/[id] ALREADY supports `before`/cursor + limit; only the chat UIs need a "load older" control), browse (done in 1.6).
- [x] 5.5 Request form server validation parity: canonical `PROVINCES` constant (fixed 13-vs-10 mismatch); province/timeline/incomeType enum validation on BOTH POST and PATCH; PATCH now also enum-checks mortgageCategory (was 500ing on malformed values); active-cap+create in one Serializable tx; draft-restore income-year bug (H-21) fixed — year selectors derive from resolved `form` state so restored amounts no longer vanish on refresh. +4 tests. REMAINING (low): parse currency strings → numbers before DB store (display-string storage is cosmetic, not breaking), free-text maxLength attrs.
- [x] 5.6 (partial) Stripe: replayed/late `invoice.paid` now early-returns on an EXPIRED/CANCELLED local subscription AND when Stripe's live status isn't active/trialing — can no longer resurrect a cancelled sub or re-grant paid credits (test assertion inverted from documenting-the-bug to asserting-the-fix). REMAINING: admin-credit-overwrite-on-renewal semantics, entitlement check on messages for lapsed brokers (product decision), subscription status map completeness (trialing/incomplete/paused).
- [ ] 5.7 Admin Mediums: settings page field/API mismatch + numeric validation, undo-toast flush on unload, ban/suspend cascade (pull requests from marketplace, close conversations), credit-adjust UI. ~~remove fake checks~~ **H-12 DONE (batch 6):** fabricated "자동 체크" results replaced with an honest manual-review checklist (no fake pass markers); RecommendedAction's false claims corrected (notification claims now actually true after batch 5).
- [x] 5.8 (mostly) PostHog now boots opted-out + memory-only; bilingual `ConsentBanner` (accept → localStorage+opt-in, decline → stays cookieless) mounted in _app; analytics now identify on opaque publicId, never raw email (login + signup). REMAINING **[USER]**: privacy page effective date + 180-day retention + homepage-display disclosure (content/legal decision).

## Stage 6 — Cleanup & tests (Lows; do opportunistically)

- [x] 6.1 (mostly) Dead code: removed RequestCard.tsx (dashboard has its own inline one), TrendChart.tsx + Tooltip.tsx (unimported), and the now-unused `recharts` dependency; untracked mortly.pptx / posthog-setup-report.md / test-results (+ .gitignore). REMAINING (trivial): lib/constants.ts mirror, `_count.reviews` phantom field, dead broker.introMessage* locale keys.
- [x] 6.2 (mostly) Security lows: verification codes now stored SHA-256 hashed (signup+resend store hash, verify hashes input to compare); forgot-password per-account 60s cooldown (silent, no enumeration oracle); signup email-failure surfaced (verify page shows resend prompt via emailFailed=1); generateVerificationCode off-by-one fixed (999999 now reachable). REMAINING (low): broader enumeration-oracle response unification.
- [x] 6.3 (partial) Missing tests added: close-from-terminal guard, conversation request-status guard + PAST_DUE guard, CLOSED-conversation send, request validation (province/timeline/income), broker filter (new shape). REMAINING: users/me deletion suite, draft-restore UI test.
- [x] 6.4 (mostly) UI lows: removed `· PRO` chip suffix, removed fake always-green admin status, `$undefined` commercial-value guard, IME `isComposing` guard on both chat sends, de-duplicated mobile menu (Dashboard/Messages no longer rendered twice; badge moved to primary nav), pristine-form `beforeunload` (only warns when dirty), OG image path (→ og-default-1.png). REMAINING (low): sitemap /en exclusions, sw.js cache versioning.

---

## Status log
- 2026-06-13 (latency investigation): user reported ~2-5s page latency. Traced it: it's `next dev` on-demand compilation, NOT a code regression — same routes measured 2-30ms on a production build (`/` was 1.25s in dev vs 3.7ms in prod). The ~30 changed files broadly invalidated the dev compile cache, so each route recompiles on first visit. Found + fixed ONE real config bug surfaced by the prod boot: `localeDetection: true` is invalid in Next 16 (only `false` allowed) — it logged a config warning and was ignored. Changed to OMIT localeDetection (= default "on", same intended behavior: NEXT_LOCALE cookie honored + EN-browser → /en), warning gone. **A dev-server restart is needed for the config change to take effect** (next.config isn't HMR'd).
- 2026-06-13 (batch 11 — big push, all 5 clusters): 5.8 PostHog consent + PII fix; 6.2 security lows (code hashing across signup/resend/verify, forgot-password cooldown, signup email-fail surfacing, randomInt fix); 6.4 UI lows (·PRO chip, fake green status, $undefined, IME guard ×2, mobile dup menu, beforeunload dirty-gate, OG image); 6.1 dead code (RequestCard/TrendChart/Tooltip + recharts removed; artifacts untracked); ConsultationStepper expired-completed bug; 6.3 guard tests. **tsc 0, lint 0 errors, 517/517 green, prod build 50/50 pages.** Remaining are all low/trivial or USER content/legal items (privacy-page retention wording, lib/constants mirror, sitemap /en exclusions, sw.js versioning, users/me deletion test). Core fix plan effectively COMPLETE — only Stage 0 user verifications block launch.
- 2026-06-13 (batch 10): 5.6 replayed-invoice.paid guard (+ payment-failed notification null-safety), 5.5 remainder (PATCH enum validation incl. mortgageCategory, H-21 draft-restore year fix). **tsc 0, lint 0 errors, 517/517 green.** Next: 5.8 PostHog consent (PIPEDA, H-18), Stage 6 cleanup (dead code, repo hygiene, security lows, missing-test backfill).
- 2026-06-13 (batch 9): 5.5 mostly (canonical PROVINCES constant fixes the 13-vs-10 mismatch across borrower form + 3 broker surfaces; province/timeline/incomeType server validation; Serializable count+create tx kills the active-cap race). +4 tests, mocked checkRateLimit in the requests integration file (it accumulates in-memory across the file → false 429s past 10 POSTs). **tsc 0, lint 0 errors, 517/517 green.** Next: 5.5 remainder (PATCH validation, currency-as-number, draft-restore H-21), 5.6 Stripe mediums, 5.8 PostHog consent, Stage 6 cleanup/tests.
- 2026-06-13 (batch 8): 5.3 (closed-conversation send guard + isSystem rendering both chat UIs), resend-code false-success fix, admin people list error-vs-empty. **tsc 0, lint 0 errors, 513/513 green** (+1 messaging test). Discovered: chat history pagination is already implemented server-side (cursor/before/limit) — only a UI "load older" control is missing. Next: 5.4 (UI load-older + borrower context limit), 5.5 (request-form server validation parity), 5.6 (Stripe mediums), 5.8 (PostHog consent), Stage 6 cleanup.
- 2026-06-13 (batch 7): 5.1 error-vs-empty (both data contexts + both dashboards), 5.2 infinite-skeleton fix. **tsc 0, lint 0 errors, 512/512 green.** Next: resend-code false-success, admin list error states, 5.3 closed-conversation send guard + isSystem rendering, 5.4 pagination, 5.5 form validation parity, 5.6 Stripe mediums, 5.8 PostHog consent, Stage 6.
- 2026-06-13 (batch 6): 4.5 (auto-close system messages + nudges + exact borrowerMsgCount engagement filter; expiry notices/push with dedupe; cron tests updated), 4.6 Navbar part (shared NoticeList + mobile sheet), and **H-12 fake admin checks removed** (caught during bookkeeping — had been wrongly counted as done). **tsc 0, lint 0 errors, 512/512 green. STAGE 4 COMPLETE — all 12 code launch blockers now genuinely closed.** Next: 3.3 completion (API error codes), Stage 5 robustness (5.1 error-vs-empty states first), Stage 6 cleanup.
- 2026-06-13 (batch 5): 4.1 (POST /api/admin/notices + user-detail modal), 4.2 (approve/reject → notice+push+email via new lib/notify.ts + sendLifecycleEmail), 4.3 (broker verification decisions). **tsc 0, lint 0 errors, 512/512 green.** Stage 4 remaining: 4.5 (cron-event notices + broken engagement filter), 4.6 (mobile bell). Then 3.3 completion, Stage 5, Stage 6.
- 2026-06-13 (batch 4): 3.4 locale plumbing (Btn→Link, reset-email locale, NEXT_LOCALE cookie + localeDetection:true behavior change, 8 en-CA date sites via lib/format.ts), 4.4 payment-failed notification (bilingual email + idempotent AdminNotice — first notice writer). 3.3 partially done (error codes on new endpoints). **tsc 0, lint 0 errors, 512/512 green.** ALL 11 code launch blockers now done; remaining blockers are user-action items 0.1/0.2. Next: Stage 4 rest (4.1 approve/reject + 4.2 notices API/modal + 4.3 broker-verification notices + 4.5 cron noise + 4.6 mobile bell), then 3.3 completion, then Stage 5.
- 2026-06-13 (batch 3): 3.1 (97 user-facing keys backfilled ko+en; admin.* deferred by decision), 3.2 (scan script + CI key-coverage test + CI gates lint/tsc/build + fixed broken `next lint`). **lint 0 errors, tsc clean, 512/512 vitest green, prod build passes locally.** Stage 3 remaining: 3.3 (API error codes), 3.4 (locale plumbing: reset-email locale, Btn→Link, NEXT_LOCALE persistence, en-CA dates, system-message localization). Then Stage 4 (notifications).
- 2026-06-13 (batch 2): 0.4, 2.1, 2.3, 2.5 (pricing.tsx part), 2.6 implemented. Stripe mock gained `subscriptionSchedules`; create-checkout tests updated (+1 new). **tsc clean, 510/510 vitest green.** Stage 2 remaining: 4.4 payment-fail notification (lives in Stage 4). Next: Stage 3 (i18n backfill + CI key test) — the largest remaining blocker.
- 2026-06-13: Plan created. Implemented: 0.3 (appleId migration), 1.1 (blank new.tsx + callbackUrl in both shells), 1.2 (Close Request UI + terminal-status guard + ko/en keys), 1.3 (diff-based PATCH + scoped-edit notice + EDIT_LOCKED_BY_CONVERSATIONS code), 1.4 (REQUEST_NOT_OPEN guard, both branches, pre-credit-spend), 1.5 partial (expiry exclusion; auto-IN_PROGRESS deferred — see product decision above), 1.6 (hasMyConversation filter + Load-more pagination + test fixtures), 2.2 (PAST_DUE/EXPIRED block in conversation create), 2.4 (billing error states + 4 locale keys + blank-page fix). Shell tests updated for callbackUrl. **tsc clean, 509/509 vitest green.** Changes uncommitted at session end — commit before continuing. Next up: 0.4, 2.1 (Stripe downgrade rework), 2.3, 2.5 (pricing honesty), 2.6, then Stage 3.
